{
  "name": "car-booking-app-root",
  "version": "1.0.0",
  "description": "Root package for Railway Deployment",
  "scripts": {
    "postinstall": "cd server && npm install",
    "build": "cd server && npm install",
    "start": "cd server && npm start"
  }
}
